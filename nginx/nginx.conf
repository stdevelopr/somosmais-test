server {
    
    listen 80;

    location / {
            root  /usr/share/nginx/html;
            index  index.html index.htm;
        }

    location /clients {
        include uwsgi_params;
        uwsgi_pass flask:8080;
    }
}